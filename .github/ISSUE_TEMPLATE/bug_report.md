---
name: Bug 报告
about: 网关行为不符合预期
title: "[Bug] 简述现象"
labels: bug
---

**环境**（必填）
- Windows 版本：
- Python 版本（`python --version`）：
- CC Switch 版本：
- Office 版本：
- 网关版本（`/healthz` 里的 version）：

**现象**
简述做了什么、期望什么、实际发生什么。

**验收结果**
```
.\verify.ps1 的输出（粘贴）
```

**日志**
`runtime\last-upstream-error.txt` 与 `runtime\edge-sup.err` 的相关片段。
⚠️ 粘贴前删掉其中的 API Key / 令牌。

**补充**
任意上下文。
